spring:
  application:
    name: loan
  datasource:
    url: jdbc:postgresql://localhost:5433/loan_db
    username: loan_admin
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081